# Runtime dependencies. Exact direct versions are intentionally pinned; CI and
# deployments install the fully resolved requirements.lock generated through
# requirements-dev.txt. ESP-IDF/esptool is installed separately in the hardware
# workstation toolchain so its platform-specific cryptography constraints do
# not enter the network-service environment.
openai==3.1.0
paho-mqtt==2.1.0
requests==2.34.2
python-dotenv==1.2.3
fastapi==0.141.1
uvicorn[standard]==0.52.3
